# Package repair v4.0.1

The previous download was truncated and lacked its ZIP central directory. This archive was rebuilt from the complete project folder using standard DEFLATE compression and conventional ZIP headers.

The launcher, all 20 available test pages, and Word Problems now use the case-sensitive password **Sat800**. Existing sessions authenticated with the old password no longer unlock the new pages. Progress keys and test content were preserved.

Extract the entire archive into a new folder, then open index.html. Do not open individual HTML files inside the compressed folder.
